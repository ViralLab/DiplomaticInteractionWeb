/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  distDir: '.next',
  trailingSlash: true
}

export default nextConfig
